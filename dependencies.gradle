ext {
    versions = [:]
    libraries = [:]
}

versions.junit = '4.12'
versions.hamcrest = '1.3'

libraries.test = [
    "junit:junit:${versions.junit}",
    "org.hamcrest:hamcrest-all:${versions.hamcrest}"
]